'use client';

export default function SystemStatusBanner() {
  // API key warnings moved to Settings > Developer tab only
  // Hidden from main dashboard to avoid confusing non-developer users
  return null;
}
