'use client';

export function SystemStatusBanner() {
  // API key warnings moved to Settings > Developer tab
  // Hidden from all pages to avoid confusing non-developer users
  return null;
}

// Keep default export for backward compatibility
export default SystemStatusBanner;
